{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "version": 2,
  "cli": "nx",
  "title": "Package executor",
  "description": "",
  "type": "object",
  "properties": {
    "arch": { "type": "string", "description": "The target arch" },
    "dir": { "type": "string", "description": "The path to the app to package" },
    "interactive": { "type": "boolean", "description": "Whether to use sensible defaults or prompt the user visually" },
    "outDir": { "type": "string", "description": "The path to the output directory for packaged apps" },
    "platform": { "type": "string", "description": "The target platform" }
  },
  "required": []
}
